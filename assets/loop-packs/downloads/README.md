# Downloads Folder

Binary ZIPs are not stored in git.

Generate them locally with:

```bash
python assets/loop-packs/generate_loop_packs.py
```

or download free-source loops with:

```bash
python assets/loop-packs/download_free_loops.py
```

Then copy resulting `*.zip` files into this `downloads/` folder for local hosting.
